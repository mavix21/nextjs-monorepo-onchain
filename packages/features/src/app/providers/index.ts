/**
 * App Providers
 *
 * Reusable application-level providers that can be shared across apps.
 * Platform-specific providers (Farcaster MiniApp, Lemon SDK) should remain in their respective apps.
 */

export { ThemeProvider } from "./theme-provider";
