/**
 * Connect Wallet Feature
 *
 * Example feature slice demonstrating FSD structure.
 * This feature handles wallet connection state and UI.
 *
 * Segments:
 * - ui/    → React components for wallet connection
 * - model/ → Types, state, business logic
 * - api/   → Wallet API interactions (not needed for this feature)
 * - lib/   → Feature-specific utilities
 */

// UI exports
export * from "./ui";

// Model exports (types, hooks for state)
export * from "./model";

// Lib exports (utilities)
export * from "./lib";
