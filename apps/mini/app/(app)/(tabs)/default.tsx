/**
 * Default fallback for the children slot.
 *
 * When navigating to a sheet-only route (like /settings via hard navigation),
 * Next.js needs something to render for `children`. This shows home content.
 */
export { default } from "./(home)/page";
